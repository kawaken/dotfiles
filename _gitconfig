[user]
    name = 
    email = 
[core]
    pager = less -R
    excludesfile = ~/.gitignore-global
    autocrlf = input
    quotepath = false
[color]
    ui = true
[alias]

    ad = add
    br = branch
    ci = commit
    cl = config --list
    co = checkout
    df = diff
    hist = log --pretty=oneline --graph --date=short
    in = fetch --dry-run
    last = log -1 HEAD
    lg = log --pretty=short --graph
    log-all = log --graph --all --decorate
    ls = ls-files
    out = push -n -v
    pul = pull --ff-only
    pus = push
    rv = remote -v
    st = status -s
    us = reset HEAD # unstage
    ex = exclude

[branch]
    autosetuprebase = always
[github]
    user = kawaken
