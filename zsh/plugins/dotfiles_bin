# dotfilesのbinディレクトリをPATHに追加
if [ -d "$HOME/dotfiles/bin" ]; then
  export PATH="$HOME/dotfiles/bin:$PATH"
fi

# git関連のbinディレクトリをPATHに追加
if [ -d "$HOME/dotfiles/git/bin" ]; then
  export PATH="$HOME/dotfiles/git/bin:$PATH"
fi